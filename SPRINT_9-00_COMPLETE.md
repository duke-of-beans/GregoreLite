Sprint S9-00 — Schema Migrations: Added two new tables to schema.sql — manifest_templates (stores saved ManifestBuilder templates with name, task_type, title, description, success_criteria, project_path, use_count) and ghost_preferences (stores Ghost positive reinforcement preferences with source_type, topic_hint, boost_factor, use_count). Both tables use CREATE TABLE IF NOT EXISTS with appropriate indexes (idx_manifest_templates_task, idx_ghost_preferences_source). No changes to database.ts runMigrations() were needed since these are new tables, not ALTER TABLE additions. Gate results: tsc --noEmit 0 errors, pnpm test:run 40 files / 890 tests / 0 failures. Files modified: app/lib/kernl/schema.sql.